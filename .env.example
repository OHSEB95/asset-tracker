# Firebase 콘솔(https://console.firebase.google.com) > 프로젝트 설정 > 일반에서 확인 가능한
# 값들을 복사해서 이 파일을 .env로 복사한 뒤 채워넣으세요. .env는 git에 커밋되지 않습니다.
# (Firestore/Auth REST API만 직접 호출하므로 이 두 값이면 충분합니다.)
MAIN_VITE_FIREBASE_API_KEY=
MAIN_VITE_FIREBASE_PROJECT_ID=
